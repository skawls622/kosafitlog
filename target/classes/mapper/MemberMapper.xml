<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kosa.fitlog.member.mapper.MemberMapper">

    <resultMap id="memberResultMap" type="com.kosa.fitlog.member.dto.MemberDTO">
        <id property="memberId" column="member_id"/>
        <result property="loginId" column="login_id"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="email" column="email"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByLoginId" resultMap="memberResultMap">
        SELECT member_id, login_id, password, nickname, email, created_at
        FROM members
        WHERE login_id = #{loginId}
    </select>

    <insert id="insertMember" parameterType="com.kosa.fitlog.member.dto.MemberDTO">
        INSERT INTO members (member_id, login_id, password, nickname, email)
        VALUES (SEQ_MEMBERS.NEXTVAL, #{loginId}, #{password}, #{nickname}, #{email,jdbcType=VARCHAR})
    </insert>
</mapper>
